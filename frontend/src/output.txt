Folder PATH listing for volume Windows
Volume serial number is 7064-2969
C:.
¶   index.html
¶   main.server.ts
¶   main.ts
¶   output.txt
¶   script.bat
¶   server.ts
¶   styles.css
¶   
+---app
¶   ¶   app.config.server.ts
¶   ¶   app.config.ts
¶   ¶   app.css
¶   ¶   app.html
¶   ¶   app.routes.server.ts
¶   ¶   app.routes.ts
¶   ¶   app.spec.ts
¶   ¶   app.ts
¶   ¶   auth.guard.ts
¶   ¶   auth.interceptor.ts
¶   ¶   role.guard.ts
¶   ¶   
¶   +---services
¶           auth.service.ts
¶           
+---components
¶   +---dealer-form
¶   ¶       dealer-form.css
¶   ¶       dealer-form.html
¶   ¶       dealer-form.spec.ts
¶   ¶       dealer-form.ts
¶   ¶       
¶   +---dealer-list
¶   ¶       dealer-list.css
¶   ¶       dealer-list.html
¶   ¶       dealer-list.spec.ts
¶   ¶       dealer-list.ts
¶   ¶       
¶   +---hello
¶   ¶       hello.css
¶   ¶       hello.html
¶   ¶       hello.ts
¶   ¶       
¶   +---login
¶   ¶       login.css
¶   ¶       login.html
¶   ¶       login.ts
¶   ¶       
¶   +---model-form
¶   ¶       model-form.css
¶   ¶       model-form.html
¶   ¶       model-form.ts
¶   ¶       
¶   +---model-list
¶   ¶       model-list.css
¶   ¶       model-list.html
¶   ¶       model-list.ts
¶   ¶       
¶   +---stock-form
¶   ¶       stock-form.css
¶   ¶       stock-form.html
¶   ¶       stock-form.ts
¶   ¶       
¶   +---stock-list
¶           stock-list.css
¶           stock-list.html
¶           stock-list.ts
¶           
+---services
        dealer.spec.ts
        dealer.ts
        model.service.ts
        stock.service.ts
        
==== C:\Users\2013288\Downloads\demo (1)\frontend\src\styles.css ====
@import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap');

*, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

:root {
  --bg: #0d0f18;
  --surface: #161929;
  --surface2: #1e2235;
  --surface3: #252a40;
  --border: #2a2f4a;
  --border-light: #333859;
  --text: #e2e8f8;
  --text-muted: #6b7499;
  --text-dim: #9aa3c4;
  --accent: #5b7fff;
  --accent-hover: #7b9aff;
  --accent-glow: rgba(91, 127, 255, 0.18);
  --danger: #f05c7a;
  --danger-bg: rgba(240, 92, 122, 0.1);
  --success: #34d399;
  --success-bg: rgba(52, 211, 153, 0.1);
  --warning: #f59e0b;
  --warning-bg: rgba(245, 158, 11, 0.1);
  --radius: 10px;
  --radius-lg: 14px;
  --shadow: 0 4px 24px rgba(0,0,0,0.4);
}

html, body {
  height: 100%;
  background: var(--bg);
  color: var(--text);
  font-family: 'Inter', 'Segoe UI', sans-serif;
  font-size: 15px;
  line-height: 1.5;
  -webkit-font-smoothing: antialiased;
}

a { color: var(--accent); text-decoration: none; }
a:hover { color: var(--accent-hover); }

::-webkit-scrollbar { width: 6px; height: 6px; }
::-webkit-scrollbar-track { background: var(--surface); }
::-webkit-scrollbar-thumb { background: var(--border-light); border-radius: 3px; }

@keyframes spin { to { transform: rotate(360deg); } }

.spinner {
  width: 30px; height: 30px;
  border: 3px solid var(--border);
  border-top-color: var(--accent);
  border-radius: 50%;
  animation: spin 0.7s linear infinite;
}

.spinner-sm {
  width: 13px; height: 13px;
  border: 2px solid rgba(255,255,255,0.3);
  border-top-color: #fff;
  border-radius: 50%;
  animation: spin 0.6s linear infinite;
}

.alert-error {
  background: var(--danger-bg);
  border: 1px solid rgba(240,92,122,0.3);
  color: var(--danger);
  padding: 0.75rem 1rem;
  border-radius: var(--radius);
  font-size: 0.875rem;
}

.alert-success {
  background: var(--success-bg);
  border: 1px solid rgba(52,211,153,0.3);
  color: var(--success);
  padding: 0.75rem 1rem;
  border-radius: var(--radius);
  font-size: 0.875rem;
}
==== C:\Users\2013288\Downloads\demo (1)\frontend\src\index.html ====
<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <title>Frontend</title>
  <base href="/">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="icon" type="image/x-icon" href="favicon.ico">
</head>
<body>
  <app-root></app-root>
</body>
</html>
==== C:\Users\2013288\Downloads\demo (1)\frontend\src\app\app.css ====
==== C:\Users\2013288\Downloads\demo (1)\frontend\src\app\app.html ====
<router-outlet />
==== C:\Users\2013288\Downloads\demo (1)\frontend\src\components\dealer-form\dealer-form.css ====
:host {
  display: block;
  min-height: 100vh;
  background: var(--bg);
  font-family: 'Inter', 'Segoe UI', sans-serif;
  color: var(--text);
}

.form-page {
  min-height: 100vh;
  display: flex; align-items: flex-start; justify-content: center;
  padding: 3rem 1.5rem;
}

.form-card {
  width: 100%; max-width: 640px;
  background: var(--surface); border: 1px solid var(--border);
  border-radius: 18px; overflow: hidden;
}

.form-header {
  padding: 2rem 2rem 1.5rem;
  border-bottom: 1px solid var(--border);
}

.back-btn {
  display: inline-block; color: var(--text-muted); font-size: 0.83rem;
  text-decoration: none; margin-bottom: 1rem; transition: color 0.2s;
}
.back-btn:hover { color: var(--accent); }

.form-title { font-size: 1.4rem; font-weight: 700; letter-spacing: -0.02em; margin-bottom: 0.3rem; }
.form-subtitle { color: var(--text-muted); font-size: 0.85rem; }

.alert-error {
  margin: 1rem 2rem 0;
  padding: 0.7rem 1rem;
  background: var(--danger-bg); border: 1px solid rgba(240,92,122,0.3);
  border-radius: var(--radius); color: var(--danger); font-size: 0.85rem;
}

.form-body { padding: 2rem; }

.form-row {
  display: grid; grid-template-columns: 1fr 1fr; gap: 1.25rem; margin-bottom: 1.25rem;
}

.field { display: flex; flex-direction: column; gap: 0.4rem; }
.full-width { margin-bottom: 1.25rem; }

label {
  font-size: 0.75rem; font-weight: 600; color: var(--text-dim);
  letter-spacing: 0.05em; text-transform: uppercase;
}
.required { color: var(--danger); }

input {
  background: var(--surface2); border: 1px solid var(--border); border-radius: var(--radius);
  padding: 0.65rem 0.9rem; font-size: 0.9rem; color: var(--text);
  outline: none; transition: border-color 0.2s, box-shadow 0.2s; width: 100%;
}
input:focus { border-color: var(--accent); box-shadow: 0 0 0 3px var(--accent-glow); }
input::placeholder { color: var(--text-muted); }

.form-actions {
  display: flex; justify-content: flex-end; gap: 0.75rem;
  margin-top: 2rem; padding-top: 1.5rem; border-top: 1px solid var(--border);
}

.btn {
  padding: 0.6rem 1.5rem; border-radius: var(--radius); border: none; cursor: pointer;
  font-size: 0.9rem; font-weight: 600; transition: all 0.2s;
  display: inline-flex; align-items: center; gap: 0.4rem;
}
.btn-cancel { background: var(--surface2); color: var(--text-muted); border: 1px solid var(--border); }
.btn-cancel:hover { color: var(--text); }
.btn-save { background: var(--accent); color: #fff; }
.btn-save:hover:not(:disabled) { background: var(--accent-hover); transform: translateY(-1px); }
.btn-save:disabled { opacity: 0.6; cursor: not-allowed; }

.spinner-sm {
  width: 13px; height: 13px; border: 2px solid rgba(255,255,255,0.3);
  border-top-color: #fff; border-radius: 50%; animation: spin 0.6s linear infinite;
}
@keyframes spin { to { transform: rotate(360deg); } }

@media (max-width: 520px) { .form-row { grid-template-columns: 1fr; } }
==== C:\Users\2013288\Downloads\demo (1)\frontend\src\components\dealer-form\dealer-form.html ====
<div class="form-page">
  <div class="form-card">
    <div class="form-header">
      <a routerLink="/dealers" class="back-btn">‚Üê Back to Dealers</a>
      <h2 class="form-title">{{ id ? 'Edit Dealer' : 'Add Dealer' }}</h2>
      <p class="form-subtitle">{{ id ? 'Update dealership information' : 'Register a new dealership' }}</p>
    </div>

    <div *ngIf="error" class="alert-error">{{ error }}</div>

    <form (ngSubmit)="save()" class="form-body">
      <div class="form-row">
        <div class="field">
          <label for="dealerName">Dealer Name <span class="required">*</span></label>
          <input id="dealerName" [(ngModel)]="dealer.dealerName" name="dealerName"
                 placeholder="e.g. City Motors Pvt Ltd" required />
        </div>
        <div class="field">
          <label for="phone">Phone</label>
          <input id="phone" [(ngModel)]="dealer.phone" name="phone" placeholder="+91 98765 43210" />
        </div>
      </div>

      <div class="field full-width">
        <label for="email">Email</label>
        <input id="email" [(ngModel)]="dealer.email" name="email" type="email" placeholder="contact@dealer.com" />
      </div>

      <div class="field full-width">
        <label for="address">Address</label>
        <input id="address" [(ngModel)]="dealer.address" name="address" placeholder="Street address" />
      </div>

      <div class="form-row">
        <div class="field">
          <label for="cityId">City ID</label>
          <input id="cityId" [(ngModel)]="dealer.cityId" name="cityId" type="number" placeholder="City ID" />
        </div>
        <div class="field">
          <label for="stateId">State ID</label>
          <input id="stateId" [(ngModel)]="dealer.stateId" name="stateId" type="number" placeholder="State ID" />
        </div>
      </div>

      <div class="form-actions">
        <button type="button" class="btn btn-cancel" (click)="cancel()">Cancel</button>
        <button type="submit" class="btn btn-save" [disabled]="saving">
          <span *ngIf="saving" class="spinner-sm"></span>
          {{ saving ? 'Saving‚Ä¶' : (id ? 'Update Dealer' : 'Add Dealer') }}
        </button>
      </div>
    </form>
  </div>
</div>
==== C:\Users\2013288\Downloads\demo (1)\frontend\src\components\dealer-list\dealer-list.css ====
:host {
  display: block;
  min-height: 100vh;
  background: var(--bg);
  font-family: 'Inter', 'Segoe UI', sans-serif;
  color: var(--text);
}

.page-wrapper { max-width: 1100px; margin: 0 auto; padding: 2rem 1.5rem; }

.page-header {
  display: flex; align-items: center; justify-content: space-between;
  margin-bottom: 1.75rem; flex-wrap: wrap; gap: 1rem;
}

.header-left { display: flex; align-items: center; gap: 1rem; flex-wrap: wrap; }

.back-link {
  font-size: 0.82rem;
  color: var(--text-muted);
  text-decoration: none;
  transition: color 0.2s;
}
.back-link:hover { color: var(--accent); }

.page-title {
  font-size: 1.6rem; font-weight: 700; letter-spacing: -0.02em;
  background: linear-gradient(135deg, #fff 0%, var(--accent) 100%);
  -webkit-background-clip: text; -webkit-text-fill-color: transparent; background-clip: text;
}

.record-count {
  font-size: 0.78rem; color: var(--text-muted);
  background: var(--surface2); padding: 0.2rem 0.6rem; border-radius: 999px;
}

.header-actions { display: flex; gap: 0.5rem; }

.btn {
  padding: 0.5rem 1.1rem; border-radius: var(--radius); border: none; cursor: pointer;
  font-size: 0.875rem; font-weight: 500; transition: all 0.2s;
}
.btn-primary { background: var(--accent); color: #fff; }
.btn-primary:hover { background: var(--accent-hover); transform: translateY(-1px); }
.btn-ghost { background: transparent; color: var(--text-muted); border: 1px solid var(--border); }
.btn-ghost:hover { color: var(--text); }

.table-container {
  background: var(--surface); border: 1px solid var(--border);
  border-radius: var(--radius-lg); overflow: hidden;
}

.data-table { width: 100%; border-collapse: collapse; }

.data-table thead tr { background: var(--surface2); }

.data-table th {
  padding: 0.8rem 1.1rem; text-align: left; font-size: 0.7rem; font-weight: 600;
  letter-spacing: 0.08em; text-transform: uppercase; color: var(--text-muted);
  border-bottom: 1px solid var(--border);
}

.table-row { border-bottom: 1px solid var(--border); transition: background 0.15s; }
.table-row:last-child { border-bottom: none; }
.table-row:hover { background: var(--surface2); }

.data-table td { padding: 0.85rem 1.1rem; font-size: 0.875rem; vertical-align: middle; }

.name-cell { font-weight: 600; }
.code-cell { font-family: 'Courier New', monospace; font-size: 0.8rem; color: var(--text-muted); }
.contact-cell div { line-height: 1.5; }
.sub-text { font-size: 0.78rem; color: var(--text-muted); }

.status-badge {
  display: inline-block; padding: 0.2rem 0.6rem; border-radius: 999px;
  font-size: 0.7rem; font-weight: 700; letter-spacing: 0.04em;
  background: var(--danger-bg); color: var(--danger);
}
.status-badge.active { background: var(--success-bg); color: var(--success); }

.actions-cell { display: flex; gap: 0.4rem; }

.btn-action {
  padding: 0.28rem 0.7rem; border-radius: 6px; border: none; cursor: pointer;
  font-size: 0.75rem; font-weight: 500; transition: all 0.2s;
}
.btn-edit { background: rgba(91,127,255,0.12); color: var(--accent); }
.btn-edit:hover { background: rgba(91,127,255,0.25); }
.btn-delete { background: var(--danger-bg); color: var(--danger); }
.btn-delete:hover { background: rgba(240,92,122,0.22); }

.empty-state, .loading-state {
  text-align: center; color: var(--text-muted); padding: 3rem !important;
}
.loading-state { display: table-cell; }

.spinner {
  width: 28px; height: 28px; border: 3px solid var(--border);
  border-top-color: var(--accent); border-radius: 50%;
  animation: spin 0.7s linear infinite; margin: 0 auto;
}
@keyframes spin { to { transform: rotate(360deg); } }
==== C:\Users\2013288\Downloads\demo (1)\frontend\src\components\dealer-list\dealer-list.html ====
<div class="page-wrapper">
  <header class="page-header">
    <div class="header-left">
      <a routerLink="/hello" class="back-link">‚Üê Dashboard</a>
      <h1 class="page-title">Dealers</h1>
      <span class="record-count" *ngIf="(dealers$ | async) as list">{{ list.length }} dealers</span>
    </div>
    <div class="header-actions">
      <button routerLink="/dealers/add" class="btn btn-primary">+ Add Dealer</button>
      <button (click)="logout()" class="btn btn-ghost">Sign out</button>
    </div>
  </header>

  <div class="table-container">
    <table class="data-table">
      <thead>
        <tr>
          <th>Dealer</th>
          <th>Code</th>
          <th>Contact</th>
          <th>Location</th>
          <th>Status</th>
          <th>Actions</th>
        </tr>
      </thead>
      <tbody>
        <ng-container *ngIf="dealers$ | async as list; else loading">
          <tr *ngFor="let dealer of list" class="table-row">
            <td class="name-cell">{{ dealer.dealerName }}</td>
            <td class="code-cell">{{ dealer.dealerCode }}</td>
            <td class="contact-cell">
              <div>{{ dealer.phone }}</div>
              <div class="sub-text">{{ dealer.email }}</div>
            </td>
            <td class="location-cell">
              <div class="sub-text">City {{ dealer.cityId }} ¬∑ State {{ dealer.stateId }}</div>
            </td>
            <td>
              <span class="status-badge" [class.active]="dealer.status === 'ACTIVE'">
                {{ dealer.status ?? 'ACTIVE' }}
              </span>
            </td>
            <td class="actions-cell">
              <button [routerLink]="['/dealers/edit', dealer.dealerId]" class="btn-action btn-edit">Edit</button>
              <button (click)="deleteDealer(dealer.dealerId!)" class="btn-action btn-delete">Delete</button>
            </td>
          </tr>
          <tr *ngIf="list.length === 0">
            <td colspan="6" class="empty-state">No dealers found. Add one to get started.</td>
          </tr>
        </ng-container>
        <ng-template #loading>
          <tr><td colspan="6" class="loading-state"><div class="spinner"></div></td></tr>
        </ng-template>
      </tbody>
    </table>
  </div>
</div>
==== C:\Users\2013288\Downloads\demo (1)\frontend\src\components\hello\hello.css ====
:host { display: block; min-height: 100vh; }

.nav {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 0 2rem;
  height: 56px;
  border-bottom: 1px solid var(--border);
  background: var(--surface);
  position: sticky;
  top: 0;
  z-index: 10;
}

.nav-brand { display: flex; align-items: center; gap: 0.5rem; }
.nav-logo { font-size: 1.25rem; }
.nav-name { font-size: 1rem; font-weight: 700; letter-spacing: -0.02em; }

.nav-right { display: flex; align-items: center; gap: 0.75rem; }

.nav-role {
  font-size: 0.7rem; font-weight: 700; letter-spacing: 0.08em; text-transform: uppercase;
  color: var(--accent); background: var(--accent-glow);
  border: 1px solid rgba(91,127,255,0.25); padding: 0.2rem 0.65rem; border-radius: 999px;
}

.btn-signout {
  background: transparent; border: 1px solid var(--border); color: var(--text-muted);
  padding: 0.35rem 0.9rem; border-radius: var(--radius); cursor: pointer;
  font-size: 0.82rem; font-family: inherit; transition: all 0.2s;
}
.btn-signout:hover { color: var(--text); border-color: var(--border-light); }

.main { max-width: 960px; margin: 0 auto; padding: 3rem 1.5rem 4rem; }

.hero { margin-bottom: 3rem; }

.hero-heading {
  font-size: 2.6rem; font-weight: 700; letter-spacing: -0.04em;
  line-height: 1.1; margin-bottom: 0.5rem;
}
.dot { color: var(--accent); }
.hero-sub { color: var(--text-muted); font-size: 1rem; }

.section-label {
  font-size: 0.7rem; font-weight: 700; letter-spacing: 0.1em; text-transform: uppercase;
  color: var(--text-muted); margin-bottom: 1rem;
}

.module-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(240px, 1fr));
  gap: 1rem;
}

.module-card {
  --card-color: var(--accent);
  display: flex; flex-direction: column; gap: 0.6rem;
  background: var(--surface); border: 1px solid var(--border); border-radius: 16px;
  padding: 1.5rem; text-decoration: none; color: var(--text);
  transition: all 0.2s; position: relative; overflow: hidden;
}
.module-card::before {
  content: ''; position: absolute; top: 0; left: 0; right: 0;
  height: 2px; background: var(--card-color); opacity: 0; transition: opacity 0.2s;
}
.module-card:hover {
  border-color: var(--card-color); background: var(--surface2);
  transform: translateY(-3px); box-shadow: 0 12px 32px rgba(0,0,0,0.25);
}
.module-card:hover::before { opacity: 1; }

.card-top { display: flex; align-items: center; justify-content: space-between; margin-bottom: 0.4rem; }
.card-icon { font-size: 1.75rem; }
.card-arrow {
  font-size: 1rem; color: var(--text-muted); transition: all 0.2s;
  opacity: 0; transform: translate(-4px, 4px);
}
.module-card:hover .card-arrow { opacity: 1; transform: translate(0,0); color: var(--card-color); }

.card-name { font-size: 1rem; font-weight: 600; }
.card-desc { font-size: 0.82rem; color: var(--text-muted); line-height: 1.5; }

@media (max-width: 480px) {
  .hero-heading { font-size: 2rem; }
  .module-grid { grid-template-columns: 1fr; }
}
==== C:\Users\2013288\Downloads\demo (1)\frontend\src\components\hello\hello.html ====
<div class="dashboard">

  <!-- Top nav -->
  <header class="nav">
    <div class="nav-brand">
      <span class="nav-logo">üöó</span>
      <span class="nav-name">DMS</span>
    </div>
    <div class="nav-right">
      <div class="nav-role">{{ role }}</div>
      <button (click)="logout()" class="btn-signout">Sign out</button>
    </div>
  </header>

  <main class="main">

    <!-- Hero -->
    <section class="hero">
      <div class="hero-text">
        <h1 class="hero-heading">Welcome back<span class="dot">.</span></h1>
        <p class="hero-sub">Here's an overview of your workspace.</p>
      </div>
    </section>

    <!-- Module cards -->
    <section class="modules">
      <h2 class="section-label">Modules</h2>
      <div class="module-grid">
        <a *ngFor="let m of visibleModules"
           [routerLink]="m.link"
           class="module-card"
           [style.--card-color]="m.color">
          <div class="card-top">
            <span class="card-icon">{{ m.icon }}</span>
            <span class="card-arrow">‚Üó</span>
          </div>
          <div class="card-name">{{ m.name }}</div>
          <div class="card-desc">{{ m.desc }}</div>
        </a>
      </div>
    </section>

  </main>
</div>
==== C:\Users\2013288\Downloads\demo (1)\frontend\src\components\login\login.css ====
:host {
  display: block;
  min-height: 100vh;
  background: var(--bg);
}

.login-page {
  min-height: 100vh;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 1.5rem;
  background: radial-gradient(ellipse at 50% 0%, rgba(91,127,255,0.12) 0%, transparent 70%);
}

.login-card {
  width: 100%;
  max-width: 400px;
  background: var(--surface);
  border: 1px solid var(--border);
  border-radius: 20px;
  padding: 2.5rem 2rem;
  box-shadow: var(--shadow);
}

.login-brand {
  text-align: center;
  margin-bottom: 2rem;
}

.brand-icon {
  font-size: 2.5rem;
  margin-bottom: 0.5rem;
}

.brand-name {
  font-size: 1.75rem;
  font-weight: 700;
  letter-spacing: -0.03em;
  background: linear-gradient(135deg, #fff 0%, var(--accent) 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}

.brand-sub {
  color: var(--text-muted);
  font-size: 0.85rem;
  margin-top: 0.25rem;
}

.login-form { display: flex; flex-direction: column; gap: 1.1rem; }

.field { display: flex; flex-direction: column; gap: 0.4rem; }

label {
  font-size: 0.78rem;
  font-weight: 600;
  color: var(--text-dim);
  letter-spacing: 0.04em;
  text-transform: uppercase;
}

input {
  background: var(--surface2);
  border: 1px solid var(--border);
  border-radius: var(--radius);
  padding: 0.7rem 1rem;
  font-size: 0.95rem;
  color: var(--text);
  outline: none;
  transition: border-color 0.2s, box-shadow 0.2s;
  width: 100%;
}

input:focus {
  border-color: var(--accent);
  box-shadow: 0 0 0 3px var(--accent-glow);
}

input::placeholder { color: var(--text-muted); }

.alert-error {
  background: var(--danger-bg);
  border: 1px solid rgba(240, 92, 122, 0.3);
  color: var(--danger);
  padding: 0.65rem 0.9rem;
  border-radius: var(--radius);
  font-size: 0.85rem;
}

.btn-login {
  margin-top: 0.5rem;
  padding: 0.75rem;
  background: var(--accent);
  color: #fff;
  border: none;
  border-radius: var(--radius);
  font-size: 0.95rem;
  font-weight: 600;
  cursor: pointer;
  transition: all 0.2s;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 0.5rem;
}

.btn-login:hover:not(:disabled) {
  background: var(--accent-hover);
  transform: translateY(-1px);
  box-shadow: 0 4px 16px rgba(91,127,255,0.35);
}

.btn-login:disabled { opacity: 0.6; cursor: not-allowed; }

.spinner-sm {
  width: 14px; height: 14px;
  border: 2px solid rgba(255,255,255,0.3);
  border-top-color: #fff;
  border-radius: 50%;
  animation: spin 0.6s linear infinite;
}

@keyframes spin { to { transform: rotate(360deg); } }
==== C:\Users\2013288\Downloads\demo (1)\frontend\src\components\login\login.html ====
<div class="login-page">
  <div class="login-card">
    <div class="login-brand">
      <div class="brand-icon">üöó</div>
      <h1 class="brand-name">DMS</h1>
      <p class="brand-sub">Dealer Management System</p>
    </div>

    <form (ngSubmit)="onSubmit()" class="login-form">
      <div class="field">
        <label for="username">Username</label>
        <input id="username" type="text" [(ngModel)]="username" name="username"
               placeholder="Enter your username" required autocomplete="username" />
      </div>

      <div class="field">
        <label for="password">Password</label>
        <input id="password" type="password" [(ngModel)]="password" name="password"
               placeholder="Enter your password" required autocomplete="current-password" />
      </div>

      <div *ngIf="errorMessage" class="alert-error">{{ errorMessage }}</div>

      <button type="submit" class="btn-login" [disabled]="loading">
        <span *ngIf="loading" class="spinner-sm"></span>
        {{ loading ? 'Signing in‚Ä¶' : 'Sign In' }}
      </button>
    </form>
  </div>
</div>
==== C:\Users\2013288\Downloads\demo (1)\frontend\src\components\model-form\model-form.css ====
:host {
  display: block;
  min-height: 100vh;
  background: var(--bg);
  font-family: 'Inter', 'Segoe UI', sans-serif;
  color: var(--text);
}

.form-page {
  min-height: 100vh;
  display: flex; align-items: flex-start; justify-content: center;
  padding: 3rem 1.5rem;
}

.form-card {
  width: 100%; max-width: 660px;
  background: var(--surface); border: 1px solid var(--border);
  border-radius: 18px; overflow: hidden;
}

.form-header {
  padding: 2rem 2rem 1.5rem; border-bottom: 1px solid var(--border);
}

.back-btn {
  display: inline-block; color: var(--text-muted); font-size: 0.83rem;
  text-decoration: none; margin-bottom: 1rem; transition: color 0.2s;
}
.back-btn:hover { color: var(--accent); }

.form-title { font-size: 1.4rem; font-weight: 700; letter-spacing: -0.02em; margin-bottom: 0.3rem; }
.form-subtitle { color: var(--text-muted); font-size: 0.85rem; }

.alert-error {
  margin: 1rem 2rem 0; padding: 0.7rem 1rem;
  background: var(--danger-bg); border: 1px solid rgba(240,92,122,0.3);
  border-radius: var(--radius); color: var(--danger); font-size: 0.85rem;
}

.form-body { padding: 2rem; }

.form-row {
  display: grid; grid-template-columns: 1fr 1fr; gap: 1.25rem; margin-bottom: 1.25rem;
}

.field { display: flex; flex-direction: column; gap: 0.4rem; }
.full-width { margin-bottom: 1.25rem; }

label {
  font-size: 0.75rem; font-weight: 600; color: var(--text-dim);
  letter-spacing: 0.05em; text-transform: uppercase;
}
.required { color: var(--danger); }

input, select {
  background: var(--surface2); border: 1px solid var(--border); border-radius: var(--radius);
  padding: 0.65rem 0.9rem; font-size: 0.9rem; color: var(--text);
  outline: none; transition: border-color 0.2s, box-shadow 0.2s; width: 100%;
}
input:focus, select:focus { border-color: var(--accent); box-shadow: 0 0 0 3px var(--accent-glow); }
input::placeholder { color: var(--text-muted); }
select option { background: var(--surface2); }

.form-actions {
  display: flex; justify-content: flex-end; gap: 0.75rem;
  margin-top: 2rem; padding-top: 1.5rem; border-top: 1px solid var(--border);
}

.btn {
  padding: 0.6rem 1.5rem; border-radius: var(--radius); border: none; cursor: pointer;
  font-size: 0.9rem; font-weight: 600; transition: all 0.2s;
  display: inline-flex; align-items: center; gap: 0.4rem;
}
.btn-cancel { background: var(--surface2); color: var(--text-muted); border: 1px solid var(--border); }
.btn-cancel:hover { color: var(--text); }
.btn-save { background: var(--accent); color: #fff; }
.btn-save:hover:not(:disabled) { background: var(--accent-hover); transform: translateY(-1px); }
.btn-save:disabled { opacity: 0.6; cursor: not-allowed; }

.spinner-sm {
  width: 13px; height: 13px; border: 2px solid rgba(255,255,255,0.3);
  border-top-color: #fff; border-radius: 50%; animation: spin 0.6s linear infinite;
}
@keyframes spin { to { transform: rotate(360deg); } }

@media (max-width: 520px) { .form-row { grid-template-columns: 1fr; } }
==== C:\Users\2013288\Downloads\demo (1)\frontend\src\components\model-form\model-form.html ====
<div class="form-page">
  <div class="form-card">
    <div class="form-header">
      <a routerLink="/models" class="back-btn">‚Üê Back to Models</a>
      <h2 class="form-title">{{ isEdit ? 'Edit Model' : 'Add New Model' }}</h2>
      <p class="form-subtitle">{{ isEdit ? 'Update vehicle model details' : 'Register a new vehicle model in the catalog' }}</p>
    </div>

    <div *ngIf="error" class="alert-error">{{ error }}</div>

    <form (ngSubmit)="save()" class="form-body">
      <div class="form-row">
        <div class="field">
          <label for="modelName">Model Name <span class="required">*</span></label>
          <input id="modelName" [(ngModel)]="model.modelName" name="modelName"
                 placeholder="e.g. Swift, Creta" required />
        </div>
        <div class="field">
          <label for="variant">Variant</label>
          <input id="variant" [(ngModel)]="model.variant" name="variant"
                 placeholder="e.g. ZXi, VXi+, Alpha" />
        </div>
      </div>

      <div class="form-row">
        <div class="field">
          <label for="fuelType">Fuel Type <span class="required">*</span></label>
          <select id="fuelType" [(ngModel)]="model.fuelType" name="fuelType" required>
            <option *ngFor="let f of fuelTypes" [value]="f">{{ f }}</option>
          </select>
        </div>
        <div class="field">
          <label for="transmission">Transmission <span class="required">*</span></label>
          <select id="transmission" [(ngModel)]="model.transmission" name="transmission" required>
            <option *ngFor="let t of transmissions" [value]="t">{{ t }}</option>
          </select>
        </div>
      </div>

      <div class="form-row">
        <div class="field">
          <label for="price">Ex-Showroom Price (‚Çπ) <span class="required">*</span></label>
          <input id="price" [(ngModel)]="model.exShowroomPrice" name="exShowroomPrice"
                 type="number" min="0" placeholder="e.g. 850000" required />
        </div>
        <div class="field">
          <label for="status">Status</label>
          <select id="status" [(ngModel)]="model.status" name="status">
            <option *ngFor="let s of statuses" [value]="s">{{ s }}</option>
          </select>
        </div>
      </div>

      <div class="form-actions">
        <button type="button" class="btn btn-cancel" (click)="cancel()">Cancel</button>
        <button type="submit" class="btn btn-save" [disabled]="saving">
          <span *ngIf="saving" class="spinner-sm"></span>
          {{ saving ? 'Saving‚Ä¶' : (isEdit ? 'Update Model' : 'Create Model') }}
        </button>
      </div>
    </form>
  </div>
</div>
==== C:\Users\2013288\Downloads\demo (1)\frontend\src\components\model-list\model-list.css ====
:host { display: block; min-height: 100vh; }

.page-wrapper { max-width: 1100px; margin: 0 auto; padding: 2rem 1.5rem; }

.page-header {
  display: flex; align-items: center; justify-content: space-between;
  margin-bottom: 1.75rem; flex-wrap: wrap; gap: 1rem;
}
.header-left { display: flex; align-items: center; gap: 1rem; flex-wrap: wrap; }

.back-link { font-size: 0.82rem; color: var(--text-muted); text-decoration: none; transition: color 0.2s; }
.back-link:hover { color: var(--accent); }

.page-title {
  font-size: 1.6rem; font-weight: 700; letter-spacing: -0.02em;
  background: linear-gradient(135deg, #fff 0%, var(--accent) 100%);
  -webkit-background-clip: text; -webkit-text-fill-color: transparent; background-clip: text;
}
.record-count {
  font-size: 0.78rem; color: var(--text-muted);
  background: var(--surface2); padding: 0.2rem 0.6rem; border-radius: 999px;
}
.header-actions { display: flex; gap: 0.5rem; align-items: center; flex-wrap: wrap; }

.btn {
  padding: 0.5rem 1.1rem; border-radius: var(--radius); border: none; cursor: pointer;
  font-size: 0.875rem; font-weight: 500; transition: all 0.2s;
}
.btn-primary { background: var(--accent); color: #fff; }
.btn-primary:hover { background: var(--accent-hover); transform: translateY(-1px); }
.btn-secondary { background: var(--surface2); color: var(--text); border: 1px solid var(--border); }
.btn-secondary:hover { background: var(--surface3); }
.btn-ghost { background: transparent; color: var(--text-muted); border: 1px solid var(--border); }
.btn-ghost:hover { color: var(--text); }

.state-loading { display: flex; flex-direction: column; align-items: center; padding: 5rem; gap: 1rem; color: var(--text-muted); }

.table-container { background: var(--surface); border: 1px solid var(--border); border-radius: var(--radius-lg); overflow: hidden; }
.data-table { width: 100%; border-collapse: collapse; }
.data-table thead tr { background: var(--surface2); }
.data-table th {
  padding: 0.8rem 1.1rem; text-align: left; font-size: 0.7rem; font-weight: 600;
  letter-spacing: 0.08em; text-transform: uppercase; color: var(--text-muted);
  border-bottom: 1px solid var(--border);
}
.table-row { border-bottom: 1px solid var(--border); transition: background 0.15s; }
.table-row:last-child { border-bottom: none; }
.table-row:hover { background: var(--surface2); }
.data-table td { padding: 0.9rem 1.1rem; font-size: 0.875rem; vertical-align: middle; }

.model-cell { display: flex; flex-direction: column; gap: 0.15rem; }
.model-name { font-weight: 600; }
.model-variant { font-size: 0.75rem; color: var(--text-muted); }
.price-cell { font-family: 'Courier New', monospace; font-weight: 600; color: var(--success); }

.badge { display: inline-block; padding: 0.2rem 0.6rem; border-radius: 999px; font-size: 0.7rem; font-weight: 700; letter-spacing: 0.04em; }
.badge-petrol { background: rgba(91,127,255,0.12); color: var(--accent); }
.badge-diesel { background: var(--warning-bg); color: var(--warning); }
.badge-ev     { background: var(--success-bg); color: var(--success); }
.badge-active   { background: var(--success-bg); color: var(--success); }
.badge-inactive { background: var(--danger-bg); color: var(--danger); }

.actions-cell { display: flex; gap: 0.4rem; }
.btn-action { padding: 0.28rem 0.7rem; border-radius: 6px; border: none; cursor: pointer; font-size: 0.75rem; font-weight: 500; transition: all 0.2s; }
.btn-edit { background: rgba(91,127,255,0.12); color: var(--accent); }
.btn-edit:hover { background: rgba(91,127,255,0.25); }
.btn-delete { background: var(--danger-bg); color: var(--danger); }
.btn-delete:hover { background: rgba(240,92,122,0.22); }

.empty-state { text-align: center; color: var(--text-muted); padding: 3rem !important; }
==== C:\Users\2013288\Downloads\demo (1)\frontend\src\components\model-list\model-list.html ====
<div class="page-wrapper">
  <header class="page-header">
    <div class="header-left">
      <a routerLink="/hello" class="back-link">‚Üê Dashboard</a>
      <h1 class="page-title">Vehicle Models</h1>
      <span class="record-count">{{ models.length }} models</span>
    </div>
    <div class="header-actions">
      <button *ngIf="isAdmin()" routerLink="/models/add" class="btn btn-primary">+ Add Model</button>
      <button routerLink="/stock" *ngIf="role === 'ADMIN' || role === 'DEALER'" class="btn btn-secondary">Stock</button>
      <button (click)="logout()" class="btn btn-ghost">Sign out</button>
    </div>
  </header>

  <div *ngIf="error" class="alert-error">{{ error }}</div>

  <div *ngIf="loading" class="state-loading">
    <div class="spinner"></div>
    <p>Loading models‚Ä¶</p>
  </div>

  <div *ngIf="!loading && !error" class="table-container">
    <table class="data-table">
      <thead>
        <tr>
          <th>Model / Variant</th>
          <th>Fuel</th>
          <th>Transmission</th>
          <th>Ex-Showroom Price</th>
          <th>Status</th>
          <th *ngIf="isAdmin()">Actions</th>
        </tr>
      </thead>
      <tbody>
        <tr *ngFor="let m of models" class="table-row">
          <td class="model-cell">
            <span class="model-name">{{ m.modelName }}</span>
            <span class="model-variant">{{ m.variant }}</span>
          </td>
          <td><span class="badge" [ngClass]="fuelBadgeClass(m.fuelType)">{{ m.fuelType }}</span></td>
          <td>{{ m.transmission }}</td>
          <td class="price-cell">‚Çπ{{ m.exShowroomPrice | number:'1.0-0' }}</td>
          <td><span class="badge" [ngClass]="statusBadgeClass(m.status ?? '')">{{ m.status }}</span></td>
          <td *ngIf="isAdmin()" class="actions-cell">
            <button [routerLink]="['/models/edit', m.modelId]" class="btn-action btn-edit">Edit</button>
            <button (click)="delete(m.modelId!)" class="btn-action btn-delete">Delete</button>
          </td>
        </tr>
        <tr *ngIf="models.length === 0">
          <td [attr.colspan]="isAdmin() ? 6 : 5" class="empty-state">No models found.</td>
        </tr>
      </tbody>
    </table>
  </div>
</div>
==== C:\Users\2013288\Downloads\demo (1)\frontend\src\components\stock-form\stock-form.css ====
:host {
  display: block;
  min-height: 100vh;
  background: var(--bg);
  font-family: 'Inter', 'Segoe UI', sans-serif;
  color: var(--text);
}

.form-page {
  min-height: 100vh;
  display: flex; align-items: flex-start; justify-content: center;
  padding: 3rem 1.5rem;
}

.form-card {
  width: 100%; max-width: 700px;
  background: var(--surface); border: 1px solid var(--border);
  border-radius: 18px; overflow: hidden;
}

.form-header {
  padding: 2rem 2rem 1.5rem; border-bottom: 1px solid var(--border);
}

.back-btn {
  display: inline-block; color: var(--text-muted); font-size: 0.83rem;
  text-decoration: none; margin-bottom: 1rem; transition: color 0.2s;
}
.back-btn:hover { color: var(--accent); }

.form-title { font-size: 1.4rem; font-weight: 700; letter-spacing: -0.02em; margin-bottom: 0.3rem; }
.form-subtitle { color: var(--text-muted); font-size: 0.85rem; }

.alert-error {
  margin: 1rem 2rem 0; padding: 0.7rem 1rem;
  background: var(--danger-bg); border: 1px solid rgba(240,92,122,0.3);
  border-radius: var(--radius); color: var(--danger); font-size: 0.85rem;
}

.form-body { padding: 2rem; }

.form-row {
  display: grid; grid-template-columns: 1fr 1fr; gap: 1.25rem; margin-bottom: 1.25rem;
}

.field { display: flex; flex-direction: column; gap: 0.4rem; }
.full-width { margin-bottom: 1.25rem; }

label {
  font-size: 0.75rem; font-weight: 600; color: var(--text-dim);
  letter-spacing: 0.05em; text-transform: uppercase;
}
.required { color: var(--danger); }
.field-hint { font-size: 0.73rem; color: var(--text-muted); margin-top: 0.1rem; }

input, select {
  background: var(--surface2); border: 1px solid var(--border); border-radius: var(--radius);
  padding: 0.65rem 0.9rem; font-size: 0.9rem; color: var(--text);
  outline: none; transition: border-color 0.2s, box-shadow 0.2s; width: 100%;
}
input:focus, select:focus { border-color: var(--accent); box-shadow: 0 0 0 3px var(--accent-glow); }
input::placeholder { color: var(--text-muted); }
select option { background: var(--surface2); }

.read-only-field {
  background: var(--surface2); border: 1px solid var(--border); border-radius: var(--radius);
  padding: 0.65rem 0.9rem; font-size: 0.9rem; color: var(--text-muted); font-style: italic;
}

.form-actions {
  display: flex; justify-content: flex-end; gap: 0.75rem;
  margin-top: 2rem; padding-top: 1.5rem; border-top: 1px solid var(--border);
}

.btn {
  padding: 0.6rem 1.5rem; border-radius: var(--radius); border: none; cursor: pointer;
  font-size: 0.9rem; font-weight: 600; transition: all 0.2s;
  display: inline-flex; align-items: center; gap: 0.4rem;
}
.btn-cancel { background: var(--surface2); color: var(--text-muted); border: 1px solid var(--border); }
.btn-cancel:hover { color: var(--text); }
.btn-save { background: var(--accent); color: #fff; }
.btn-save:hover:not(:disabled) { background: var(--accent-hover); transform: translateY(-1px); }
.btn-save:disabled { opacity: 0.6; cursor: not-allowed; }

.spinner-sm {
  width: 13px; height: 13px; border: 2px solid rgba(255,255,255,0.3);
  border-top-color: #fff; border-radius: 50%; animation: spin 0.6s linear infinite;
}
@keyframes spin { to { transform: rotate(360deg); } }

@media (max-width: 520px) { .form-row { grid-template-columns: 1fr; } }
==== C:\Users\2013288\Downloads\demo (1)\frontend\src\components\stock-form\stock-form.html ====
<div class="form-page">
  <div class="form-card">
    <div class="form-header">
      <a routerLink="/stock" class="back-btn">‚Üê Back to Stock</a>
      <h2 class="form-title">Add Vehicle Stock</h2>
      <p class="form-subtitle">Register a new vehicle to the inventory</p>
    </div>

    <div *ngIf="error" class="alert-error">{{ error }}</div>

    <form (ngSubmit)="save()" class="form-body">

      <!-- VIN -->
      <div class="field full-width">
        <label for="vin">VIN (Vehicle Identification Number) <span class="required">*</span></label>
        <input id="vin" [(ngModel)]="request.vin" name="vin"
               placeholder="17-character VIN e.g. 1HGCM82633A123456" required
               maxlength="50" />
        <span class="field-hint">Must be unique across all stock</span>
      </div>

      <div class="form-row">
        <!-- Model -->
        <div class="field">
          <label for="modelId">Vehicle Model <span class="required">*</span></label>
          <select id="modelId" [(ngModel)]="request.modelId" name="modelId" required>
            <option [value]="0" disabled>Select model‚Ä¶</option>
            <option *ngFor="let m of models" [value]="m.modelId">
              {{ m.modelName }} {{ m.variant }} ‚Äî {{ m.fuelType }}
            </option>
          </select>
        </div>

        <!-- Color -->
        <div class="field">
          <label for="color">Color</label>
          <input id="color" [(ngModel)]="request.color" name="color"
                 placeholder="e.g. Pearl White, Midnight Black" />
        </div>
      </div>

      <div class="form-row">
        <!-- Manufacture Date -->
        <div class="field">
          <label for="mfgDate">Manufacture Date</label>
          <input id="mfgDate" [(ngModel)]="request.manufactureDate" name="manufactureDate"
                 type="date" />
        </div>

        <!-- Dealer selection: Admin only -->
        <div *ngIf="isAdmin()" class="field">
          <label for="dealerId">Assign to Dealer <span class="required">*</span></label>
          <select id="dealerId" [(ngModel)]="selectedDealerId" name="dealerId" required>
            <option [value]="null" disabled>Select dealer‚Ä¶</option>
            <option *ngFor="let d of dealers" [value]="d.dealerId">{{ d.dealerName }}</option>
          </select>
        </div>

        <!-- Dealer note for non-admin -->
        <div *ngIf="!isAdmin()" class="field">
          <label>Assigned Dealer</label>
          <div class="read-only-field">Derived from your account (auto)</div>
          <span class="field-hint">Your dealership ID is securely extracted from your login session</span>
        </div>
      </div>

      <div class="form-actions">
        <button type="button" class="btn btn-cancel" (click)="cancel()">Cancel</button>
        <button type="submit" class="btn btn-save" [disabled]="saving">
          <span *ngIf="saving" class="spinner-sm"></span>
          {{ saving ? 'Adding‚Ä¶' : 'Add to Inventory' }}
        </button>
      </div>
    </form>
  </div>
</div>
==== C:\Users\2013288\Downloads\demo (1)\frontend\src\components\stock-list\stock-list.css ====
:host { display: block; min-height: 100vh; }

.page-wrapper { max-width: 1300px; margin: 0 auto; padding: 2rem 1.5rem; }

.page-header {
  display: flex; align-items: center; justify-content: space-between;
  margin-bottom: 1.5rem; flex-wrap: wrap; gap: 1rem;
}
.header-left { display: flex; align-items: center; gap: 1rem; flex-wrap: wrap; }

.back-link { font-size: 0.82rem; color: var(--text-muted); text-decoration: none; transition: color 0.2s; }
.back-link:hover { color: var(--accent); }

.page-title {
  font-size: 1.6rem; font-weight: 700; letter-spacing: -0.02em;
  background: linear-gradient(135deg, #fff 0%, var(--accent) 100%);
  -webkit-background-clip: text; -webkit-text-fill-color: transparent; background-clip: text;
}
.record-count {
  font-size: 0.78rem; color: var(--text-muted);
  background: var(--surface2); padding: 0.2rem 0.6rem; border-radius: 999px;
}
.header-actions { display: flex; gap: 0.5rem; align-items: center; flex-wrap: wrap; }

.btn {
  padding: 0.5rem 1.1rem; border-radius: var(--radius); border: none; cursor: pointer;
  font-size: 0.875rem; font-weight: 500; transition: all 0.2s;
  display: inline-flex; align-items: center; gap: 0.3rem;
}
.btn-primary { background: var(--accent); color: #fff; }
.btn-primary:hover:not(:disabled) { background: var(--accent-hover); transform: translateY(-1px); }
.btn-primary:disabled { opacity: 0.5; cursor: not-allowed; }
.btn-secondary { background: var(--surface2); color: var(--text); border: 1px solid var(--border); }
.btn-secondary:hover { background: var(--surface3); }
.btn-ghost { background: transparent; color: var(--text-muted); border: 1px solid var(--border); }
.btn-ghost:hover { color: var(--text); }
.btn-cancel { background: var(--surface2); color: var(--text-muted); border: 1px solid var(--border); }

.filter-bar { display: flex; align-items: center; gap: 0.5rem; margin-bottom: 1.25rem; flex-wrap: wrap; }
.filter-label { font-size: 0.78rem; color: var(--text-muted); font-weight: 600; }
.filter-btn {
  padding: 0.28rem 0.85rem; border-radius: 999px; border: 1px solid var(--border);
  background: var(--surface2); color: var(--text-muted); cursor: pointer;
  font-size: 0.75rem; font-weight: 500; transition: all 0.2s;
}
.filter-btn:hover { color: var(--text); }
.filter-btn.active { background: var(--accent); color: #fff; border-color: var(--accent); }

.table-container { background: var(--surface); border: 1px solid var(--border); border-radius: var(--radius-lg); overflow: hidden; }
.data-table { width: 100%; border-collapse: collapse; }
.data-table thead tr { background: var(--surface2); }
.data-table th {
  padding: 0.8rem 1rem; text-align: left; font-size: 0.7rem; font-weight: 600;
  letter-spacing: 0.08em; text-transform: uppercase; color: var(--text-muted);
  border-bottom: 1px solid var(--border);
}
.table-row { border-bottom: 1px solid var(--border); transition: background 0.15s; }
.table-row:last-child { border-bottom: none; }
.table-row:hover { background: var(--surface2); }
.data-table td { padding: 0.85rem 1rem; font-size: 0.875rem; vertical-align: middle; }

.vin-cell { font-family: 'Courier New', monospace; font-size: 0.78rem; color: var(--text-muted); }
.model-cell { display: flex; flex-direction: column; gap: 0.1rem; }
.model-name { font-weight: 600; }
.model-variant { font-size: 0.75rem; color: var(--text-muted); }
.dealer-cell { color: var(--text-muted); font-size: 0.83rem; }

.color-dot {
  display: inline-block; width: 10px; height: 10px; border-radius: 50%;
  margin-right: 0.35rem; vertical-align: middle; border: 1px solid rgba(255,255,255,0.15);
}

.status-badge { display: inline-block; padding: 0.2rem 0.6rem; border-radius: 999px; font-size: 0.7rem; font-weight: 700; letter-spacing: 0.04em; }
.status-available { background: var(--success-bg); color: var(--success); }
.status-booked    { background: var(--warning-bg); color: var(--warning); }
.status-sold      { background: rgba(107,116,153,0.12); color: var(--text-muted); }

.actions-cell { display: flex; gap: 0.35rem; flex-wrap: wrap; }
.action-btn { padding: 0.25rem 0.65rem; border-radius: 6px; border: none; cursor: pointer; font-size: 0.73rem; font-weight: 600; transition: all 0.2s; }
.btn-book     { background: rgba(91,127,255,0.12); color: var(--accent); }
.btn-book:hover { background: rgba(91,127,255,0.25); }
.btn-sell     { background: var(--success-bg); color: var(--success); }
.btn-sell:hover { background: rgba(52,211,153,0.22); }
.btn-transfer { background: var(--warning-bg); color: var(--warning); }
.btn-transfer:hover { background: rgba(245,158,11,0.22); }
.btn-delete   { background: var(--danger-bg); color: var(--danger); }
.btn-delete:hover { background: rgba(240,92,122,0.22); }

.state-loading { display: flex; flex-direction: column; align-items: center; padding: 5rem; gap: 1rem; color: var(--text-muted); }
.empty-state { text-align: center; color: var(--text-muted); padding: 3rem !important; }

.modal-overlay {
  position: fixed; inset: 0; background: rgba(0,0,0,0.7); backdrop-filter: blur(4px);
  display: flex; align-items: center; justify-content: center; z-index: 100;
}
.modal {
  background: var(--surface); border: 1px solid var(--border); border-radius: 18px;
  padding: 2rem; width: 100%; max-width: 420px; box-shadow: var(--shadow);
}
.modal-title { font-size: 1.2rem; font-weight: 700; margin: 0 0 0.3rem; }
.modal-subtitle { color: var(--text-muted); font-size: 0.875rem; margin: 0 0 1.5rem; }
.field { display: flex; flex-direction: column; gap: 0.4rem; margin-bottom: 1.25rem; }
.field label { font-size: 0.75rem; font-weight: 600; color: var(--text-dim); text-transform: uppercase; letter-spacing: 0.05em; }
.field select {
  background: var(--surface2); border: 1px solid var(--border); border-radius: var(--radius);
  padding: 0.65rem 0.9rem; font-size: 0.9rem; color: var(--text); outline: none;
}
.field select:focus { border-color: var(--accent); box-shadow: 0 0 0 3px var(--accent-glow); }
.modal-actions { display: flex; justify-content: flex-end; gap: 0.75rem; margin-top: 1.5rem; }
==== C:\Users\2013288\Downloads\demo (1)\frontend\src\components\stock-list\stock-list.html ====
<div class="page-wrapper">

  <!-- Header -->
  <header class="page-header">
    <div class="header-left">
      <a routerLink="/hello" class="back-link">‚Üê Dashboard</a>
      <h1 class="page-title">Vehicle Stock</h1>
      <span class="record-count">{{ filteredStocks.length }} / {{ stocks.length }} vehicles</span>
    </div>
    <div class="header-actions">
      <button routerLink="/stock/add" class="btn btn-primary">+ Add Stock</button>
      <button routerLink="/models" class="btn btn-secondary">Models</button>
      <button *ngIf="isAdmin()" routerLink="/" class="btn btn-secondary">Dealers</button>
      <button (click)="logout()" class="btn btn-ghost">Sign out</button>
    </div>
  </header>

  <!-- Alerts -->
  <div *ngIf="successMsg" class="alert-success">‚úì {{ successMsg }}</div>
  <div *ngIf="error" class="alert-error">{{ error }}</div>

  <!-- Filter bar -->
  <div class="filter-bar">
    <span class="filter-label">Filter:</span>
    <button *ngFor="let s of ['ALL','AVAILABLE','BOOKED','SOLD']"
            class="filter-btn"
            [class.active]="statusFilter === s"
            (click)="statusFilter = s; applyFilter()">
      {{ s }}
    </button>
  </div>

  <!-- Loading -->
  <div *ngIf="loading" class="state-loading">
    <div class="spinner"></div>
    <p>Loading inventory‚Ä¶</p>
  </div>

  <!-- Table -->
  <div *ngIf="!loading" class="table-container">
    <table class="data-table">
      <thead>
        <tr>
          <th>VIN</th>
          <th>Model / Variant</th>
          <th *ngIf="isAdmin()">Dealer</th>
          <th>Color</th>
          <th>Mfg Date</th>
          <th>Status</th>
          <th>Actions</th>
        </tr>
      </thead>
      <tbody>
        <tr *ngFor="let s of filteredStocks" class="table-row">
          <td class="vin-cell">{{ s.vin }}</td>
          <td class="model-cell">
            <span class="model-name">{{ s.modelName }}</span>
            <span class="model-variant">{{ s.variant }}</span>
          </td>
          <td *ngIf="isAdmin()" class="dealer-cell">{{ s.dealerName }}</td>
          <td>
            <span class="color-dot" [style.background]="s.color?.toLowerCase()"></span>
            {{ s.color }}
          </td>
          <td>{{ s.manufactureDate | date:'mediumDate' }}</td>
          <td>
            <span class="status-badge" [ngClass]="statusClass(s.stockStatus ?? '')">
              {{ s.stockStatus }}
            </span>
          </td>
          <td class="actions-cell">
            <!-- Book: only if AVAILABLE -->
            <button *ngIf="s.stockStatus === 'AVAILABLE'"
                    (click)="markBooked(s.vin)"
                    class="action-btn btn-book" title="Mark as Booked">Book</button>
            <!-- Sell: only if BOOKED -->
            <button *ngIf="s.stockStatus === 'BOOKED'"
                    (click)="markSold(s.vin)"
                    class="action-btn btn-sell" title="Mark as Sold">Sell</button>
            <!-- Transfer: only if AVAILABLE -->
            <button *ngIf="s.stockStatus === 'AVAILABLE'"
                    (click)="openTransfer(s.vin)"
                    class="action-btn btn-transfer" title="Transfer to another dealer">Transfer</button>
            <!-- Delete: ADMIN only -->
            <button *ngIf="isAdmin()"
                    (click)="deleteStock(s.vin)"
                    class="action-btn btn-delete" title="Delete">Delete</button>
          </td>
        </tr>
        <tr *ngIf="filteredStocks.length === 0 && !loading">
          <td [colSpan]="isAdmin() ? 7 : 6" class="empty-state">No vehicles found.</td>
        </tr>
      </tbody>
    </table>
  </div>

</div>

<!-- Transfer Modal -->
<div *ngIf="showTransfer" class="modal-overlay" (click)="showTransfer = false">
  <div class="modal" (click)="$event.stopPropagation()">
    <h3 class="modal-title">Transfer Stock</h3>
    <p class="modal-subtitle">VIN: <strong>{{ transferVin }}</strong></p>

    <div class="field">
      <label>Target Dealer</label>
      <select [(ngModel)]="targetDealerId" name="targetDealer">
        <option [value]="null" disabled>Select dealer‚Ä¶</option>
        <option *ngFor="let d of dealers" [value]="d.dealerId">{{ d.dealerName }}</option>
      </select>
    </div>

    <div class="modal-actions">
      <button class="btn btn-cancel" (click)="showTransfer = false">Cancel</button>
      <button class="btn btn-primary"
              [disabled]="!targetDealerId || transferring"
              (click)="confirmTransfer()">
        <span *ngIf="transferring" class="spinner-sm"></span>
        {{ transferring ? 'Transferring‚Ä¶' : 'Confirm Transfer' }}
      </button>
    </div>
  </div>
</div>
